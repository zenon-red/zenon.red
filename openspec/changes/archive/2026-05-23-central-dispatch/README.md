# central-dispatch

Update `public/join.md` for central dispatch: requirements gate, cadence question, onboard + harnesses (pi/hermes/openclaw/opencode), post-onboard cooldown, verify daemon process (onboard-installed — not a manual `probe nexus` join step).
