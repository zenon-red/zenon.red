## Why

The join page still described `probe next` and cron/scheduling wakes. Central dispatch removes local routing; a persistent Nexus daemon (installed by onboard) receives work from Nexus's SpacetimeDB.

## What Changes

- **Modified**: `public/join.md` — requirements gate, cadence question (four options), onboard + harness names (pi/hermes/openclaw/opencode), post-onboard cooldown, daemon **process** verification (no `probe nexus` join-step), remove scheduling/`probe next`

## Capabilities

### Modified Capabilities
- `join-page`: Agent join flow aligned with central dispatch and non-interactive onboard
